---
status: complete
phase: v1.0-mvp (all phases)
source: 01-01-SUMMARY.md, 01-02-SUMMARY.md, 02-01-SUMMARY.md, 02-02-SUMMARY.md, 03-01-SUMMARY.md, 03-02-SUMMARY.md, 04-01-SUMMARY.md, 04-02-SUMMARY.md, 04-03-SUMMARY.md, 05-01-SUMMARY.md, 05-02-SUMMARY.md, 05-03-SUMMARY.md
started: 2026-02-22T12:00:00Z
updated: 2026-02-22T12:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Server Starts and Loads UI
expected: Run `npm start`, open http://localhost:3000. Full-screen display loads with dark backgrounds. Three zones cycle with smooth crossfade transitions.
result: pass

### 2. Zone Rotation Timing
expected: Zones rotate at intervals matching config.yaml durations (default: wod 120s, video 180s, roster 60s). Each transition is a smooth opacity crossfade (~1 second).
result: pass

### 3. Manual Zone Advance
expected: While the display is running, visit http://localhost:3000/api/zones/advance (or POST to it). The display should immediately crossfade to the next zone.
result: pass

### 4. Health and Config API Endpoints
expected: Visit /api/health — returns JSON with status "ok" and uptime. Visit /api/config — returns JSON with zone settings but NO passwords, API keys, or tokens visible.
result: pass

### 5. Config Hot-Reload
expected: While server is running, edit a duration in config.yaml (e.g., change roster duration from 60 to 30). Within 30 seconds, the rotation picks up the new timing without restarting the server.
result: pass

### 6. WOD Zone Display States
expected: Without WodScreen credentials configured, the WOD zone shows a loading state briefly then falls back to an error/fallback state. It should never show a completely blank screen.
result: issue
reported: "Credentials are configured and it shows a popup asking to click to authorize gym instead of WOD content"
severity: major

### 7. YouTube Video Playback
expected: Add a real YouTube URL to the videos section in config.yaml (enabled: true). When the video zone activates, the YouTube player loads and plays the video with audio. No YouTube controls or annotations visible.
result: pass

### 8. Video Zone Completion
expected: After a YouTube video finishes playing, the system either plays the next video in the playlist or advances to the next zone if the playlist is exhausted.
result: pass

### 9. Reels Disabled Gracefully
expected: Without Instagram credentials, visit /api/reels — returns JSON with enabled:false, empty reels array, no errors. The video zone works fine without Reels (YouTube only).
result: pass

### 10. Roster Zone Unconfigured State
expected: Without MindBody credentials (or with placeholder "your_api_key" values), the roster zone shows a "Roster unavailable" or similar error state. It should not crash or show blank.
result: pass

### 11. Roster and MindBody API Endpoints
expected: Visit /api/roster — returns JSON with classInfo, athletes, count fields. Visit /api/schedule — returns JSON with classes array. Visit /api/mindbody/status — shows configured:false. No errors.
result: pass

### 12. Boost Field in Zone State
expected: Visit /api/zones/current — returns JSON that includes a boostActive field (should be false when MindBody is not configured).
result: pass

### 13. Production Bind Address
expected: Stop the server. Start with `NODE_ENV=production npm start`. Server should log that it's binding to 127.0.0.1 (not 0.0.0.0). localhost:3000 still works but other machines on the network cannot connect.
result: pass

### 14. Deployment Scripts Present
expected: Check scripts/ folder. Should contain: gym-display.service, gym-kiosk.service, start-kiosk.sh, hdmi-cec.sh, gym-cec-off.service, gym-cec-off.timer, gym-cec-on.service, gym-cec-on.timer, gym-display.logrotate, setup.sh.
result: pass

### 15. Setup Script Structure
expected: Open scripts/setup.sh. Should be a comprehensive bash script with: root check, Node.js install, Chromium install, CEC utils, systemd service/timer installation, autologin setup, journald limits, logrotate config, RPi Connect install, and a summary section at the end.
result: pass

## Summary

total: 15
passed: 14
issues: 1
pending: 0
skipped: 0

## Issues for /gsd:plan-fix

- UAT-001: WOD zone shows WodScreen authorization popup instead of workout content when credentials are configured (major) - Test 6
  root_cause:
